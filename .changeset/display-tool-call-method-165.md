---
"@ai-hero/sandcastle": patch
---

Add `toolCall(name, formattedArgs)` method to Display service interface. FileDisplay writes `Name(args)` as a plain log line; ClackDisplay renders a dim-styled step; SilentDisplay stores entries for test assertions.
